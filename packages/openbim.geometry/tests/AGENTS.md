# Tests

Tests must cover closed catalog typing, invalid guarantee rejection, and claims that must remain absent until backed by a concrete implementation. Keep manifests honest: an unavailable algorithm is a passing absence, not a placeholder claim.
