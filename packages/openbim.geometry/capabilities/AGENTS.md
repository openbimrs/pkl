# Capability catalog

Each module contributes only stable, vendor-neutral atomic IDs to `Catalog.pkl`. IDs describe an operation or representation—not a kernel, API, quality guarantee, or regulatory/model-checking outcome. Prefer a new scoped ID over silently broadening an existing one.
