# Schema types

`Manifest.pkl` is the implementation declaration root. `Requirements.pkl` gives downstream tools declarative matching inputs; `Conformance.pkl` defines their output, not an in-Pkl policy engine. Keep quality (`Exactness.pkl`) distinct from operation IDs and keep claims scoped to explicit dimensions and representations.
